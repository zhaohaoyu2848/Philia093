# Element-Skin 生产环境配置文件

jwt:
  secret: "prod-secret-please-change-to-a-very-long-string-in-production"  # ⚠️ 生产环境必须修改
  expire_days: 7
  access_expire_minutes: 30

# RSA 密钥配置
keys:
  private_key: "/app/data/private.pem"
  public_key: "/app/data/public.pem"

# 本站 OIDC Server 的独立 RSA 签名密钥；文件不存在时自动创建
oidc:
  private_key: "/app/data/oidc-private.pem"
  public_key: "/app/data/oidc-public.pem"

# 使用 `openssl rand -base64 32` 生成一次，配置身份后不得更换
identity:
  encryption_key: ""

database:
  host: "db"
  port: "5432"
  user: "elementskin"
  password: "password123"
  name: "elementskin"
  sslmode: "disable"
  max_connections: 20

redis:
  host: "redis"
  port: "6379"
  password: "ZHYzhy081217"
  db: 0
  key_prefix: "elementskin:"
  public_cache_ttl_seconds: 60
  auth_cache_ttl_seconds: 30

textures:
  directory: "/app/frontend/static/textures"

carousel:
  directory: "/app/frontend/static/carousel"

server:
  host: "0.0.0.0"
  port: 8000
  site_url: "http://yourdomain.com"
  api_url: "http://yourdomain.com/skinapi"

# CORS 跨域配置
cors:
  allow_origins: ["http://127.0.0.1:5173", "http://localhost:5173"]
  allow_credentials: true
